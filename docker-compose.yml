version: "3.9"
services:
  webapp:
    build:
      context: .
      dockerfile: ./Dockerfile.development
    ports:
      - "8000:80"
    volumes:
      - .:/var/www
    environment:
      - APP_ENV=development
      - APACHE_RUN_USER=#1000
      - APACHE_RUN_GROUP=#1000  
